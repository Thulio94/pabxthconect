[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
prefix = asterisk

; Local development uses ws://localhost:8088/asterisk/ws.
; Production must terminate HTTPS/WSS with a valid certificate.
