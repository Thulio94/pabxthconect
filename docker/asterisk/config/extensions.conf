[general]
static = yes
writeprotect = no

[globals]
RECORDING_ROOT = /var/spool/asterisk/monitor

; Tenant dialplans are generated by Laravel. Recording is executed by the PBX,
; never by the browser, and only on calls that reach the bridge.
#include "generated/extensions_tenants.conf"
